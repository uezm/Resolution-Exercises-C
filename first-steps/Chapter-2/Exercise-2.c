/* 2) Write a program that reads a number entire and then print it. */

/* Resolution */
#include <stdio.h>

int main()
{
    int num;
    scanf("%d", &num);
    printf("%d", num);
    return 0;
}
