/* 2) Write a program that reads a real number and prints the
fifth part of that number. */

/* Resolution */
#include <stdio.h>

int main()
{
    float num;
    scanf("%f", &num);
    printf("%f", num / 5);
    return 0;
}
